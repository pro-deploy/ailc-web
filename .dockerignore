# Исключаем из контекста сборки только лишнее. site/ и deploy/ нужны образу лендинга,
# поэтому их НЕ игнорируем. Образ MCP-сервера (./Dockerfile) ничего не копирует из
# контекста, для него размер контекста некритичен.
.git
.github
.claude
node_modules
*.log
.DS_Store
